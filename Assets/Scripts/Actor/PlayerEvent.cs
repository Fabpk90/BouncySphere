using UnityEngine;
using UnityEngine.UI;

namespace Assets.Scripts.Actor
{
    class PlayerEvent : MonoBehaviour
    {
        void Start()
        {
            /*
            GameObject canvasGO = new GameObject();
            RectTransform canvasRT = canvasGO.AddComponent<RectTransform>();
            Canvas canvasCV = canvasGO.AddComponent<Canvas>();

            Button Level = canvasGO.AddComponent<Button>();
            Level.GetComponent<GUIText>().text = "Test";

            //Level.GetComponent<GUI>()
            Level.transform.position = new Vector3(100, 100, 0);
            */

           

        }
    }
}
